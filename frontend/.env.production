# Same-origin build: SPA is served by the Django app itself, so the API is a
# relative path (no CORS, no separate service). Deployed as a single miget
# resource alongside the backend.
VITE_API_BASE=/api
VITE_WS_BASE=/ws/events/
